---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: a6858d7aaaff09cf1df8fe922bdb87d1_d0410ec7b0de11f18f50525400aeaaa3
    ReservedCode1: J2sLgrXJLKDKGKQfAzB3fBD4GqShcRxAoRFrM8EQR5Rgf1SV2+Nu+8YGBNGlPti+xPi5vWzLqcANDrS36a2twsKP5+4GQZd51NNRnDRCwyAFT6lb9teefqTtp/PrjdaozLyTtbapko4q69aeSuSAqlcT4uvgc+FPfedxAxC3XohdVUwkW2mVTSbzXqo=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: a6858d7aaaff09cf1df8fe922bdb87d1_d0410ec7b0de11f18f50525400aeaaa3
    ReservedCode2: J2sLgrXJLKDKGKQfAzB3fBD4GqShcRxAoRFrM8EQR5Rgf1SV2+Nu+8YGBNGlPti+xPi5vWzLqcANDrS36a2twsKP5+4GQZd51NNRnDRCwyAFT6lb9teefqTtp/PrjdaozLyTtbapko4q69aeSuSAqlcT4uvgc+FPfedxAxC3XohdVUwkW2mVTSbzXqo=
---

# PESE Consulting 网站部署指南

> 适用项目：PESE_Web（Vite + React 单文件站点）
> 托管平台：Firebase Hosting（项目 ID：pese-consulting-gx）
> 线上地址：https://www.peseconsulting.com （根域 www 已 CNAME 指向 pese-consulting-gx.web.app）
> 最后更新：2026-09-15

## 一、部署前准备

1. 项目目录：`D:\work\xuangang\tools\PESE Consulting\PESE_Web`
2. 构建产物目录：`dist`（部署时上传/指向的就是这个目录里的内容）
3. 部署配置：项目根目录已有 `firebase.json`（public 指向 dist，已配置 SPA 路由重写）

### 首次部署前需重新构建（源码有改动时）

在项目目录打开 PowerShell 执行：

```powershell
cd "D:\work\xuangang\tools\PESE Consulting\PESE_Web"
npm run build
```

构建成功后会生成最新的 `dist` 目录（单文件 index.html + 图片等静态资源）。

---

## 二、方式一：控制台手动上传（最简单，无需命令行登录）

### 步骤

1. 浏览器打开 Firebase Hosting 管理页：
   ```
   https://console.firebase.google.com/project/pese-consulting-gx/hosting
   ```
   （登录有权限的 Google 账号）

2. 点击页面上的 **「上传文件」** 按钮（首次部署显示为「开始使用」）。

3. 打开资源管理器进入：
   ```
   D:\work\xuangang\tools\PESE Consulting\PESE_Web\dist
   ```
   选中 **dist 里面的全部内容**（index.html、favicon、logo 文件、img 文件夹等），**不要选 dist 文件夹本身**，直接拖进上传区域。

4. 点击 **「发布 / Deploy」**，确认覆盖。

5. 等待 1~2 分钟（CDN 缓存刷新），访问验证：
   ```
   https://www.peseconsulting.com
   ```
   若仍看到旧页面，按 Ctrl+F5 强制刷新。

### 说明
- 控制台上传适合一次性、偶尔更新；发布后可在 Hosting 页面查看发布历史并一键回滚。
- 同路径文件会覆盖，旧站多余文件会保留（不影响访问）。

---

## 三、方式二：Firebase CLI 部署（推荐长期使用）

### 1. 安装 CLI（已装可跳过）

```powershell
npm install -g firebase-tools
```

### 2. 登录授权（首次需要）

```powershell
firebase login
```

- 会弹出浏览器，用拥有 `pese-consulting-gx` 项目权限的 Google 账号登录并允许授权
- 看到 `Success! Logged in as xxx@gmail.com` 即成功
- 登录态保存在本机，后续无需重复登录

### 3. 关联项目（首次需要）

在项目目录执行：

```powershell
cd "D:\work\xuangang\tools\PESE Consulting\PESE_Web"
firebase use pese-consulting-gx
```

### 4. 构建并部署

```powershell
npm run build
firebase deploy --only hosting
```

看到 `Deploy complete!` 即部署成功。

### 5. 日常更新流程（以后只需这两条）

```powershell
cd "D:\work\xuangang\tools\PESE Consulting\PESE_Web"
npm run build
firebase deploy --only hosting
```

---

## 四、部署后验证

- 主站：https://www.peseconsulting.com
- Firebase 默认域：https://pese-consulting-gx.web.app
- 两者指向同一站点，任一个能打开新页面即部署成功
- 页面有缓存时强制刷新：Ctrl + F5

---

## 五、常见问题

| 问题 | 处理 |
|---|---|
| 浏览器打开还是旧页面 | 等 1~2 分钟 + Ctrl+F5 强刷；或换无痕窗口访问 |
| firebase login 打不开浏览器 | 换用 `firebase login:ci` 生成长期 token，用 `firebase deploy --token <token>` 部署 |
| deploy 提示无权限 | 确认登录的 Google 账号已被添加为项目成员（Owner/Editor），见下方权限说明 |
| 域名打不开 | 检查 GoDaddy DNS：www 的 CNAME 必须指向 `pese-consulting-gx.web.app`，根域 A 记录指向 Firebase IP（199.36.158.100） |

## 六、权限管理（添加协作者）

Firebase 控制台 → 项目设置 → 用户和权限：
```
https://console.firebase.google.com/project/pese-consulting-gx/settings/iam
```
点「添加成员」，输入对方 Google 邮箱，授予 **Editor**（可部署代码、管理资源）或 **Owner**（含成员与计费管理）角色。对方接受邀请后即可用自己的账号部署。
*（内容由AI生成，仅供参考）*
